<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.universidadcauca.movil.preguntas" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Preguntas"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_preguntas" >
        </activity>
        <activity
            android:name=".AddActivity"
            android:label="AddActivity" >
        </activity>
        <activity
            android:name=".DrawerPreguntas"
            android:label="@string/title_activity_drawer_preguntas" >
        </activity>
        <activity
            android:name=".NavDrawerPreguntas"
            android:label="@string/title_activity_nav_drawer_preguntas" >
        </activity>
    </application>

</manifest>
